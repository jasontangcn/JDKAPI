1、本线程池为“范型线程池”，因为它向线程传递的是一个实现了Runnable接口的对象，然后调用对象的run()。
   而专用线程池一般传递特定数据给线程处理。
   处理封装在内部而不需要传递线程运行动作run()对象。

2、对于范型线程池我的设计思路：
(1)不用ThreadPoolWorker，而设计一个类继承于Thread,  用一个FIFOStack来控制工作对象即可。
(2)将FIFOStack融合到本程序中，不需要单独设计。